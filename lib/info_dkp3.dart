import 'package:flutter/material.dart';

class InfoDKP3 extends StatelessWidget {
  const InfoDKP3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Informasi DKP3"),
      ),
      body: Container(
        child: Center(child: Text("Informasi DKP3")),
      ),
    );
  }
}
